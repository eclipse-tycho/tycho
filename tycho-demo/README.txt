To checkout Tycho demo projects have been moved to a separate git repository
To checkout the demo projects execute the following command

   git clone git://git.eclipse.org/gitroot/tycho/org.eclipse.tycho-demo.git

