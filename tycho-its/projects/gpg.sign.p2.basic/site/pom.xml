<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.eclipse.tycho.maven</groupId>
		<artifactId>org.eclipse.tycho.maven.parent</artifactId>
		<version>1.0.0-SNAPSHOT</version>
		<relativePath>..</relativePath>
	</parent>

	<groupId>org.eclipse.tycho.maven</groupId>
	<artifactId>org.eclipse.tycho.maven.site</artifactId>
	<version>1.0.0-SNAPSHOT</version>
	<packaging>eclipse-repository</packaging>

	<build>
		<plugins>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-p2-repository-plugin</artifactId>
				<version>${tycho-version}</version>
				<configuration>
					<repositoryName>Tycho Maven</repositoryName>
					<includeAllDependencies>true</includeAllDependencies>
					<includeAllSources>true</includeAllSources>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>gpg-sign</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.eclipse.tycho</groupId>
						<artifactId>tycho-gpg-plugin</artifactId>
						<version>${tycho-version}</version>
						<executions>
							<execution>
								<id>sign-1</id>
								<configuration>
									<keyname>${gpg-keyname}</keyname>
									<skipIfJarsigned>${test.skipIfJarsigned}</skipIfJarsigned>
									<skipIfJarsignedAndAnchored>true</skipIfJarsignedAndAnchored>
									<skipBinaries>${test.skipBinaries}</skipBinaries>
									<forceSignature>
										<bundle>${test.forceSignature}</bundle>
									</forceSignature>
									<signer>${test.signer}</signer>
								</configuration>
								<goals>
									<goal>sign-p2-artifacts</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>gpg-sign-2</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.eclipse.tycho</groupId>
						<artifactId>tycho-gpg-plugin</artifactId>
						<version>${tycho-version}</version>
						<executions>
							<execution>
								<id>sign-2</id>
								<configuration>
									<keyname>${gpg-keyname-2}</keyname>
									<skipIfJarsignedAndAnchored>true</skipIfJarsignedAndAnchored>
									<forceSignature>
										<bundle>${test.forceSignature}</bundle>
									</forceSignature>
									<signer>${test.signer}</signer>
									<pgpKeyBehavior>${test.pgpKeyBehavior-2}</pgpKeyBehavior>
								</configuration>
								<goals>
									<goal>sign-p2-artifacts</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
