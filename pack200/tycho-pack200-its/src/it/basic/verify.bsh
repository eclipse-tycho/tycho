import java.io.*;

File feature = new File(basedir, "repository/target/repository/plugins/pack200.bundle_1.0.0.123abc.jar.pack.gz"); 
if (!feature.canRead()) {
  throw new Exception("Missing expected file "+feature);
}

return true;
